# RODOH action-evidence closure

This branch is the immutable physical-session custody candidate coordinated with:

```text
World PR #203
World receiver 5c892be5f468682f8dfd64ce4e42270d1445d6b0
Arc action authority 6eef311836ee7cb3a43a94ce51f448a2699c3b04
World physical acceptance issue #204
```

## Authority

- Arc owns action law, accepted action receipts, and campaign mutation.
- World and Unity own presentation, bounded input sampling, and provisional execution candidates.
- `axm-embodied` owns physical observation, immutable spool ingestion, journal continuity, and evidence projection.
- Genesis may preserve the final shard without acquiring action or campaign authority.

A physical safety observation must retain `campaignEffect: null`. A Unity candidate must retain `authority: Arc replay required`. Neither may claim the other's authority.

## Closed hosted boundary

The candidate includes:

- bounded duplicate-key-refusing JSON before semantic interpretation;
- exact cartridge and action-spec identity custody;
- append-only hash-chained `axm-embodied-action-session/1` journals;
- receipt-after-candidate ordering;
- resumable, transactional spool ingestion;
- sequence rollback, path escape, payload-hash, root-change, duplicate-attachment, and tamper refusal;
- Genesis-facing shard projection;
- one permanent ordinary CI supergate covering focused custody tests, the complete regression, Genesis drift checking, clean checkout, and an exact qualification receipt.

No additional event kind, sensor type, journal format, workflow, or interpretation layer belongs on this branch before physical acceptance.

## Remaining acceptance

World issue #204 is the only completion path. It requires the actual Unity `6000.0.66f2` worktree, a passing generated scene and EditMode receipt, Windows player smoke, Quest APK and device session, immutable spool, exact Arc replay, accepted action receipt, completed journal, and independent Genesis-facing shard verification.

This branch must remain draft and unmerged until those records exist. Book IV and every unrelated post-v1 program remain outside this lane.
